apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-learning.fullname" . }}
  labels:
    {{- include "kube-learning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- with .Values.service.nodePort }}
      nodePort: {{ . }}
      {{- end }}
  selector:
    {{- include "kube-learning.selectorLabels" . | nindent 4 }}
